import os
import data_structures
import numpy as np


def load_field(X,Y,U,V,S2N,frame_):

    
    if type(X) == np.ndarray and type(Y) == np.ndarray:
        if len(X.shape)==1 and len(Y.shape)==1:
            
        if len(X.shape)==2 and len(Y.shape)==2:
    prop =  vec_properties()
    vec = vector(x,y,u,vs2n,properties)
    
